module.exports = [
	0,
	-1,
	1,
	Math.pow(2, 8),
	Math.pow(2, 8) + 1,
	Math.pow(2, 8) - 1,
	Math.pow(2, 16),
	Math.pow(2, 16) + 1,
	Math.pow(2, 16) - 1,
	Math.pow(2, 32),
	Math.pow(2, 32) + 1,
	Math.pow(2, 32) - 1,
	Math.pow(2, 64),
	Math.pow(2, 64) + 1,
	Math.pow(2, 64) - 1,
	Number.MAX_VALUE,
	Number.MIN_VALUE
];
